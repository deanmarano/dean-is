
import { markdownToHtml } from 'dean-is/helpers/markdown-to-html';
import { module, test } from 'qunit';

module('Unit | Helper | markdown to html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = markdownToHtml([42]);
  assert.ok(result);
});

